// import expect from 'expect';
// import createBranchedSelector from '../branchedSelector';

// describe('utils/branchedSelector', () => {
//   it('should have recomputed getCart three times instead of seven', () => {
//     // calling getCart a few more times, but it shouldn't recompute
//     selector.getCart(state, props);
//     selector.getCart(state, props);
//     selector.getCart(state, props);
//     selector.getCart(state, props);
//     expect(
//       selector.getCart.selector.recomputations()
//     ).toEqual(3);
//   });
// });
