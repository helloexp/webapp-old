const host = 'https://test3-barcode.fastretailing.com';
const basePath = `${host}/api/v1`;

const config = {
  host,
  base: basePath,
  locale: 'jp',
  region: 'jp',
  brand: 'uq',
  url: '/img?scale=4&height=9&number=',
};

export default config;
