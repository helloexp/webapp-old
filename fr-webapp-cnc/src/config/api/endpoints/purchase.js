const host = 'https://test3-api.fastretailing.com';

const basePath = `${host}/order/v3/uq/jp`;

const config = {
  host,
  base: basePath,

  // Endpoints
  purchase: '/history',
};

export default config;
