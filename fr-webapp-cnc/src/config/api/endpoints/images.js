const host = '//im.uniqlo.com';
const baseImagesPath = `${host}/images/%region/%type/goods/`;

const config = {
  host,
  base: baseImagesPath,
  locale: 'jp',
  region: 'jp',
  type: 'pc',
  brand: 'uq',
};

export default config;
