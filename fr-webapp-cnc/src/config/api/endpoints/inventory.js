const host = 'https://endtoend15d3-api.fastretailing.com';
const path = '/order/v1/uq/jp/inventoryInquiry';
const base = host + path;

const config = {
  host,
  path,
  base,
};

export default config;
